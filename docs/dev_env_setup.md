Once you are done installing system dependencies by following [Installing System dependencies][system_dependencies].
To run the application on local machine do the following

    bundle install
    rake db:migrate
    rake db:seed
    rails s



[system_dependencies]: system_dependencies.md
